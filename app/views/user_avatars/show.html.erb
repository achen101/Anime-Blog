<h1 class="center">Choose An Avatar</h1>
<div class="form-group center">
  <% Avatar.all.each do |a| %>
  <%= link_to image_tag("#{a.image}", class: "avatar"), user_avatars_path(avatar_id: a.id), method: :post %>
  <% end %>
</div>
